import React from "react";
import { Typography } from "@material-ui/core";
import Box from "./Box";

const Home = props => {
  return (
    <Box>
      <Typography>This is sample app created to learn React</Typography>
    </Box>
  );
};

export default Home;
